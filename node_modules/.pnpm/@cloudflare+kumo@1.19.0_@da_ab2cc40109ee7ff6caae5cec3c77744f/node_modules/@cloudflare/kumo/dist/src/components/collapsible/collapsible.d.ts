import { PropsWithChildren } from 'react';
export declare const KUMO_COLLAPSIBLE_VARIANTS: {};
export declare const KUMO_COLLAPSIBLE_DEFAULT_VARIANTS: {};
export interface KumoCollapsibleVariantsProps {
}
export declare function collapsibleVariants(_props?: KumoCollapsibleVariantsProps): string;
/**
 * Collapsible component props.
 *
 * @example
 * ```tsx
 * <Collapsible label="Show details" open={open} onOpenChange={setOpen}>
 *   <Text>Hidden content revealed when expanded.</Text>
 * </Collapsible>
 * ```
 */
export type CollapsibleProps = PropsWithChildren<KumoCollapsibleVariantsProps & {
    /** Text label displayed in the trigger button */
    label: string;
    /** Whether the collapsible content is visible */
    open?: boolean;
    /** Callback fired when the open state changes */
    onOpenChange?: (open: boolean) => void;
    /** Additional CSS classes for the content panel */
    className?: string;
}>;
/**
 * Collapsible component for showing/hiding content.
 *
 * Features:
 * - Animated chevron indicator (rotates 180° when open)
 * - Accessible with aria-expanded and aria-controls
 * - Content panel with left border accent
 *
 * @example
 * ```tsx
 * const [open, setOpen] = useState(false);
 *
 * <Collapsible label="Show details" open={open} onOpenChange={setOpen}>
 *   <Text>Hidden content revealed when expanded.</Text>
 * </Collapsible>
 * ```
 *
 * @example Controlled accordion pattern
 * ```tsx
 * const [activeIndex, setActiveIndex] = useState<number | null>(null);
 *
 * {items.map((item, i) => (
 *   <Collapsible
 *     key={i}
 *     label={item.title}
 *     open={activeIndex === i}
 *     onOpenChange={(open) => setActiveIndex(open ? i : null)}
 *   >
 *     {item.content}
 *   </Collapsible>
 * ))}
 * ```
 */
export declare const Collapsible: import('react').ForwardRefExoticComponent<KumoCollapsibleVariantsProps & {
    /** Text label displayed in the trigger button */
    label: string;
    /** Whether the collapsible content is visible */
    open?: boolean;
    /** Callback fired when the open state changes */
    onOpenChange?: (open: boolean) => void;
    /** Additional CSS classes for the content panel */
    className?: string;
} & {
    children?: import('react').ReactNode | undefined;
} & import('react').RefAttributes<HTMLDivElement>>;
//# sourceMappingURL=collapsible.d.ts.map